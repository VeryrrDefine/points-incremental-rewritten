export enum Tabs {
    Main,
    Other,
    Stat,
    Reset
}

export enum StatTabs {
    Stat,
    Achievements,
}
export enum MainTabs {
    Dimensions,
    Sqrt
}

export enum OtherTabs {
    Options,
    About,
}